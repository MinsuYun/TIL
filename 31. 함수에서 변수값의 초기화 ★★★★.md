# 함수에서 변수값의 초기화 ★★★★

```javascript
--(1)var evenArray = [];가 함수 onlyEvenElements 바깥에 있을 때--

function  oddFunc(num){
  return num%2;  
}

var evenArray = [];

function onlyEvenElements(array) {
 for( var i=0 ; i<array.length ; i++ ) {
    if(!oddFunc(array[i])) {
      evenArray.push(array[i]);
    }
  }
  return evenArray;
}

var input = [3,5,6,7,7,88,9];
console.log(onlyEvenElements(input)); //[6,88]
console.log(onlyEvenElements(input)); //[6,88,6,88]
console.log(onlyEvenElements(input)); //[6,88,6,88,6,88]

```

```javascript
--(2)var evenArray = [];가 함수 onlyEvenElements 안에 있을 때--

function  oddFunc(num){
  return num%2;
}

function onlyEvenElements(array) {
var evenArray = [];
 for( var i=0 ; i<array.length ; i++ ) {
    if(!oddFunc(array[i])) {
      evenArray.push(array[i]);
    }
  }
  return evenArray;
}

var input = [3,5,6,7,7,88,9];
console.log(onlyEvenElements(input)); //[6,88]
console.log(onlyEvenElements(input)); //[6,88]
console.log(onlyEvenElements(input)); //[6,88]
```